# Agents-AI
Repositório focado em agrupar todos os agentes de IA criado.

# Agent01
Agente focado em se conectar com o banco de dados PostgreSQL e trazer informações.

python3 -m venv labenv

source labenv/bin/activate

pip install google.adk
pip install toolbox-core

- Rodar MCP-DATABASE: (Ler README do mcp-database)

adk web

# Agent02
Agente focado em se conectar com o TRELLO API e gerenciar as tarefas do dia.

pip install -r requirements.txt

adk web